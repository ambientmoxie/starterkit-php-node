# Leave empty — these values are handled programmatically by setEnv.js.
VITE_DEV=
VITE_DEV_SERVER_IP=

# Set `VITE_ROOT` to the subfolder path if the app is not hosted at the server root.
# Example: VITE_ROOT=/subfolder
# Leave empty if the app is hosted at the root.
VITE_ROOT=
