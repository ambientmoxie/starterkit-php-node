{
  "name": "starterkit-php-node",
  "description": "starterkit/php-node",
  "type": "module",
  "scripts": {
    "server": "php -S 0.0.0.0:8000 router.php",
    "vite": "vite",
    "dev": "concurrently \"node scripts/set-env.mjs && npm run server\" \"node scripts/set-env.mjs && npm run vite\" -c yellow,green",
    "build": "node scripts/set-env.mjs && vite build",
    "preview": "concurrently \"npm run build\" \"npm run server\" -c yellow,green",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "normalize.css": "^8.0.1",
    "sass": "^1.68.0",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "concurrently": "^9.1.0",
    "dotenv": "^16.4.5"
  }
}